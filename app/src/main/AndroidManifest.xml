<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MachineProject_MOBDEVE_Mercado_Sillona_Park"
        tools:targetApi="34">

        <activity
            android:name=".AddEditViewActivity"
            android:exported="false"/>

        <activity
            android:name=".CourseViewActivity"
            android:exported="false"/>

        <activity
            android:name=".SaturdayActivity"
            android:exported="false"/>

        <activity
            android:name=".FridayActivity"
            android:exported="false"/>

        <activity
            android:name=".ThursdayActivity"
            android:exported="false"/>

        <activity
            android:name=".WednesdayActivity"
            android:exported="false"/>

        <activity
            android:name=".TuesdayActivity"
            android:exported="false"/>

        <activity
            android:name=".MondayActivity"
            android:exported="false"/>

        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>